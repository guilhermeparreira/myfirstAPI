# Notes of APIs for Beginners - How to use an API (Full Course / Tutorial) - freeCodeCamp

It is a contract:

+ Define what it needs, and what it outputs.
API =~ Web based API (but it is not always the case)
WebAPIs: It is what guarantees that the code will work on all browsers.

## Interface

Abstract the way it is implemented. It is made for the user.
What goes behind the interface the user has no idea.
It is something that the user needs to understand how to use.

## Application

Any software program, app, or service that wants to perform a task or retrieve data.

## Programming

This indicates that the tool is built for software developers to write code automating interactions between systems.

### GUI (Graphical User Interface)

Buttons
Pointers
Menus
Icons

## Remote API

Your object can use a service that it is connected to an external API.

## REST
****
REST - Representational State Transfer

REST =~ API (Just like Xerox ~= photocopier)

APIs that are REST compliance are RESTful APIs:
    - Client-server architecture
    - Statelessness -> the server does not store data of client

### WEB

Browser -> Web Client -> Server

**URL: Universal Resource Identifier** is a superset term of URL: **Universal Resource Locator**

Http: Hypertext Transfer Protocol -> It is also a contract.
    - It is a stateless protocol: don't keep the memory
    - GET
    - POST
    - Header fields to handle status, cache, etc...
Cache
Hypertext: It is a non-linear digital text that uses clickable links (hyperlinks). In html is used anchor tags \<a\> a \</a\>.
On a request, it returns the body, the most important message:
Body: Has the HTML (Hyper Text Markup Language)

## Extra Tools

Twillio to build yours APIs, while Postman to test your APIs. Usually, use them together.

Thunder Client in VS Code.
Java Script is assyncronous by default, all the methods. it is a lot used in internet of things. When you define a function, you set the keyworkd `async` before the function name to make it assyncronous. And inside it, you place `await` to mention what you want to be async.

Web Hooks -> It is a reverse API. Instead of calling the API, the API calls me.
